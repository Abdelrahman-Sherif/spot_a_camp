import 'package:flutter/material.dart';

class CustomRadius {
  static const small = Radius.circular(8),
      m = Radius.circular(12),
      large = Radius.circular(24);

  static const Radius input = small;
}
